import { Button, Typography } from "@mui/material";

const Navbar = () => {
  return (
    <>
      <Button>
        <Typography>Button 1</Typography>
      </Button>
      <Button>
        <Typography>Button 1</Typography>
      </Button>
      <Button>
        <Typography>Button 1</Typography>
      </Button>
      <Button>
        <Typography>Button 1</Typography>
      </Button>
    </>
  );
};

export default Navbar;
